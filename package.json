{
  "name": "node-ts-eval",
  "version": "1.0.0",
  "description": "NODE TYPESCRIPT EVAL",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf ./dist && npx tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.1",
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "install": "^0.13.0"
  }
}
